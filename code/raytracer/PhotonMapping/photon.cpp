
// Photon::Photon(Vector p_position, Vector p_direction, Colour p_intensity, enum photonType p_type){
// 	position = p_position;
// 	direction = p_direction;
// 	intensity = p_intensity;
// 	type = p_type;
// }

// float Photon::operator[] (int axis) const{
// 	if (axis == 0) { return position.x; }
// 	if (axis == 0) { return position.y; }
// 	if (axis == 0) { return position.z; }
// }

// const Photon& Photon::point() const{
// 	return *this;
// }

// bool Photon::operator == (const Photon& p) const {
// 	return position.x == p.position.x && position.y == p.position.y && position.z == p.position.z;
// }

